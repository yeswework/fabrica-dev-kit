// Use global FDK Webpack configuration. This can be adapted to fit each project needs by changing the configuration returned by the `require()` below
module.exports = require('fabrica-dev-kit/webpack.config.js')(__dirname);
